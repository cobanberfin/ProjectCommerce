@model ProductListModel


<div class="row">
    <div class="col-md-3">
        @await Component.InvokeAsync("CategoryList")
             
          
    </div>
     @*  product sayfasında da aynı menuye sahıbı<*@

    <div col-md-9>
        
        <div class="row mt-3">
   

    @foreach(var product in Model.Products)
            {
              <partial name="_Product" for="@product" />
            }

        </div>
        <div page-model ="@Model.PageInfo">

        </div>
    </div>
</div>